class Solution(object):
    def reverseString(self, s):
        """
        :type s: List[str]
        :rtype: None Do not return anything, modify s in-place instead.
        """
        # 문자열을 뒤집는 함수를 작성하라. 입력값은 문자열 배열, 리턴 없이 리스트 내부를 직접 조작하라.
        return s.reverse()